directive @client on FIELD

type Query {
  auth: User
  isExtensionCheckDone: Boolean
}

type Mutation {
  logout: Boolean
}

input UserInput {
  id: ID!
  name: String!
  email: String!
  updatedAt: Float
}
